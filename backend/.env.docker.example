# Copy this file to `.env` when you want to run PostgreSQL in Docker.

# PostgreSQL (Docker Compose maps container 5432 -> localhost:55432)
DATABASE_URL=postgres://postgres:postgres@localhost:55432/nanny

# JWT
JWT_SECRET=replace-me-with-a-long-random-string
JWT_EXPIRES_IN=7d

# Server
PORT=4000
CORS_ORIGIN=http://localhost:3000

# Optional email delivery for order confirmations
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Payment provider
PAYMENT_PROVIDER=demo

# Optional SMS delivery through Twilio
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM=
